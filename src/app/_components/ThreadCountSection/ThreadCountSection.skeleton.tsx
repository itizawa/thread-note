import { Skeleton } from "@/shared/components/Skeleton";

export const ThreadCountSectionSkeleton = () => {
  return (
    <Skeleton variant="text" width={300} height={40} sx={{ mx: "auto" }} />
  );
};
