/* eslint-disable no-restricted-imports */
import {
  AvatarProps as MuiAvatarProps,
  Avatar as MuiAvator,
} from "@mui/material";

export const Avator = (props: MuiAvatarProps) => {
  return <MuiAvator {...props} />;
};
